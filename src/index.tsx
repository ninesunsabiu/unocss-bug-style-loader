import 'uno.css';
import ReactDOM from 'react-dom';

const Root = () => {
    return <div className='after:content-empty'>hello world</div>
}

ReactDOM.render(<Root />, document.getElementById('zw-root'));